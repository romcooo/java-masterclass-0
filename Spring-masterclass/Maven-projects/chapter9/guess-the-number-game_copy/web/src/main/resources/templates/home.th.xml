<thlogic>
    <attr
            sel="#headId"
            th:replace="~{fragments/head :: headFragment}"/>
    <attr
            sel="#play-link"
            th:href="@{__${T(com.romco.util.GameMappings).PLAY}__}"
            th:text="#{game.play.link}"/>
    <attr
            sel="#footerId"
            th:replace="~{fragments/footer :: footerFragment}"/>
    <attr
            sel="#welcome"
            th:text="#{game.welcome}"/>

</thlogic>